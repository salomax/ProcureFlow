schema
  @link(url: "https://specs.apollo.dev/link/v1.0")
  @link(url: "https://specs.apollo.dev/join/v0.3", for: EXECUTION)
{
  query: Query
  mutation: Mutation
}

directive @join__enumValue(graph: join__Graph!) repeatable on ENUM_VALUE

directive @join__field(graph: join__Graph, requires: join__FieldSet, provides: join__FieldSet, type: String, external: Boolean, override: String, usedOverridden: Boolean) repeatable on FIELD_DEFINITION | INPUT_FIELD_DEFINITION

directive @join__graph(name: String!, url: String!) on ENUM_VALUE

directive @join__implements(graph: join__Graph!, interface: String!) repeatable on OBJECT | INTERFACE

directive @join__type(graph: join__Graph!, key: join__FieldSet, extension: Boolean! = false, resolvable: Boolean! = true, isInterfaceObject: Boolean! = false) repeatable on OBJECT | INTERFACE | UNION | ENUM | INPUT_OBJECT | SCALAR

directive @join__unionMember(graph: join__Graph!, member: String!) repeatable on UNION

directive @link(url: String, as: String, for: link__Purpose, import: [link__Import]) repeatable on SCHEMA

input BaseEntityInput
  @join__type(graph: APP)
{
  name: String!
}

type CatalogItem
  @join__type(graph: APP, key: "id")
{
  id: ID!
  name: String!
  category: CatalogItemCategory!
  priceCents: Int!
  status: CatalogItemStatus!
  description: String
  createdAt: String!
  updatedAt: String!
}

enum CatalogItemCategory
  @join__type(graph: APP)
{
  MATERIAL @join__enumValue(graph: APP)
  SERVICE @join__enumValue(graph: APP)
}

input CatalogItemInput
  @join__type(graph: APP)
{
  name: String!
  category: CatalogItemCategory!
  priceCents: Int!
  status: CatalogItemStatus!
  description: String
}

enum CatalogItemStatus
  @join__type(graph: APP)
{
  ACTIVE @join__enumValue(graph: APP)
  PENDING_APPROVAL @join__enumValue(graph: APP)
  INACTIVE @join__enumValue(graph: APP)
}

input ChatInput
  @join__type(graph: ASSISTANT)
{
  sessionId: String!
  message: String!
}

type ChatResponse
  @join__type(graph: ASSISTANT)
{
  sessionId: String!
  response: String!
}

input CheckoutInput
  @join__type(graph: APP)
{
  items: [CheckoutItemInput!]!
  totalPriceCents: Int!
  itemCount: Int!
}

type CheckoutItem
  @join__type(graph: APP)
{
  catalogItemId: ID!
  name: String!
  priceCents: Int!
  quantity: Int!
}

input CheckoutItemInput
  @join__type(graph: APP)
{
  catalogItemId: ID!
  name: String!
  priceCents: Int!
  quantity: Int!
}

type CheckoutLog
  @join__type(graph: APP, key: "id")
{
  id: ID!
  userId: ID
  items: [CheckoutItem!]!
  totalPriceCents: Int!
  itemCount: Int!
  status: CheckoutStatus!
  createdAt: String!
}

enum CheckoutStatus
  @join__type(graph: APP)
{
  COMPLETED @join__enumValue(graph: APP)
  FAILED @join__enumValue(graph: APP)
}

type Conversation
  @join__type(graph: ASSISTANT)
{
  sessionId: String!
  messageCount: Int!
  lastMessage: String
}

scalar join__FieldSet

enum join__Graph {
  APP @join__graph(name: "app", url: "http://host.docker.internal:8081/graphql")
  ASSISTANT @join__graph(name: "assistant", url: "http://host.docker.internal:8082/graphql")
  SECURITY @join__graph(name: "security", url: "http://host.docker.internal:8080/graphql")
}

scalar link__Import

enum link__Purpose {
  """
  `SECURITY` features provide metadata necessary to securely resolve fields.
  """
  SECURITY

  """
  `EXECUTION` features provide metadata necessary for operation execution.
  """
  EXECUTION
}

type Mutation
  @join__type(graph: APP)
  @join__type(graph: ASSISTANT)
  @join__type(graph: SECURITY)
{
  saveCatalogItem(input: CatalogItemInput!): CatalogItem! @join__field(graph: APP)
  checkout(input: CheckoutInput!): CheckoutLog! @join__field(graph: APP)
  chat(input: ChatInput!): ChatResponse! @join__field(graph: ASSISTANT)
  clearConversation(sessionId: String!): Boolean! @join__field(graph: ASSISTANT)
  signIn(input: SignInInput!): SignInPayload! @join__field(graph: SECURITY)
}

type Query
  @join__type(graph: APP)
  @join__type(graph: ASSISTANT)
  @join__type(graph: SECURITY)
{
  searchCatalogItems(query: String!): [CatalogItem!]! @join__field(graph: APP)
  catalogItem(id: ID!): CatalogItem @join__field(graph: APP)
  conversation(sessionId: String!): Conversation @join__field(graph: ASSISTANT)
  currentUser: User @join__field(graph: SECURITY)
}

input SignInInput
  @join__type(graph: SECURITY)
{
  email: String!
  password: String!
  rememberMe: Boolean
}

type SignInPayload
  @join__type(graph: SECURITY)
{
  token: String!
  refreshToken: String
  user: User!
}

type User
  @join__type(graph: SECURITY, key: "id")
{
  id: ID!
  email: String!
  displayName: String
}